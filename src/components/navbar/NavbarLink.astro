---
interface Props {
    name: string;
    href: string;
}
const { name, href } = Astro.props;
const currentPath = Astro.url.pathname;
---

<a href={href} class={currentPath === href ? "text-rose-500 dark:text-rose-400" : "hover:text-rose-500 hover:dark:text-rose-400"}>{name}</a>
