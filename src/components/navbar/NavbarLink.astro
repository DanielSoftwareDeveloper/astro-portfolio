---
interface Props {
    name: string;
    href: string;
}
const { name, href } = Astro.props;
const currentPath = Astro.url.pathname;
---

<a href={href} class={currentPath === href ? "text-rose-400" : "hover:text-hover"}>{name}</a>
